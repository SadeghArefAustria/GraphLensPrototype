# Core — extraction + Neo4j
anthropic>=0.92.0
neo4j>=5.0.0

# ML — graph analysis and link prediction
networkx>=3.0
numpy>=1.24
scikit-learn>=1.3

# Optional heavyweight ML (install separately):
#   pip install graphlens[ml]   ← PyKEEN + PyTorch (embedding models)
#   pip install graphlens[pyg]  ← PyTorch Geometric (GNNs)